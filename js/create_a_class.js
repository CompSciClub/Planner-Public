  /* This whole section here is rather non-functional, I'm not sure how to get the selected option from the HTML. Also, all these ifs are probably horribly inefficient. $("#blockSelect").change(function (var block) {  
    block = document.getElementById("blockSelect").value;
    if(option == "A"){ //This big thing should just check the block and then send it over to whatever function we'll have doin' stuff.
      block = "A";
    };
    else if(option == "B") {
      block = "B";
    };
    else if(option == "C") {
      block = "C";
    };
    else if(option == "D") {
      block = "D";
    };
    else if(option == "E") {
      block = "E";
    };
    else if(option == "F") {
      block = "F";
    };
    else if(option == "G") {
      block = "G";
    };
    else {
      block = "H";
    };
    console.log(block); //only here for debuggery in case this doesn't work...
    setBlock (block);
  });
  
  function setBlock(block);
  */